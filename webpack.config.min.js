var webpack = require('webpack');
var fs = require('fs');

module.exports = {
    entry: __dirname + '/src/index.js',
    output: {
        filename: __dirname + '/dist/ngAugmentNativeScroll.min.js',
        library: 'ngAugmentNativeScroll',
        libraryTarget: 'umd'
    },
    module: {
        loaders: [{
            test: /\.js$/,
            exclude: /node_modules/,
            loader: 'babel'
        }]
    },
    plugins: [
        new webpack.BannerPlugin(fs.readFileSync('./LICENSE', 'utf8')),
        new webpack.optimize.UglifyJsPlugin({
            minimize: true
        })
    ],
    devtool: 'source-map',
    progress: true,
    profile: true,
    colors: true
};
